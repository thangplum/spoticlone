import React from 'react'
import Icon from '../../../icons';

interface ControlButtonProps {
    
}

export const ControlButton: React.FC<ControlButtonProps> = (props) => {
        return (
            // <button title={props.title} className={`control-button ${props.size && props.size} ${props.active? 'active':''} ${props.extraClass&&extraClass} no-outline`} onClick={props.onClick}>
            //     <Icon name={props.icon}/>
            // </button>
            <></>
        );
}